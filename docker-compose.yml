services:
  dev-env:
    build: .
    container_name: pi-vibe-env
    volumes:
      - .:/workspace
      - ./.pi:/home/pi/.pi
    ports:
      - "8000:8000"
      - "5173:5173"
    env_file:
      - .env
