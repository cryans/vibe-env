[project]
name = "transcript-extractor"
version = "0.1.0"
dependencies = [
    "youtube-transcript-api>=0.6.2",
    "yt-dlp>=2026.05.01",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
